import {curve} from '../data/project';export function CurveChart(){const w=720,h=270,p=34,x=(i:number)=>p+i*(w-2*p)/(curve.length-1),y=(v:number)=>h-p-v*(h-2*p)/100,line=(idx:number)=>curve.map((d,i)=>`${x(i)},${y(d[idx] as number)}`).join(' ');return <div className="chart-shell"><div className="head"><div><h3>Curva de avanço</h3><p>Planejado × realizado acumulado</p></div><div className="legend"><span>Planejado</span><b>Realizado</b></div></div><svg viewBox={`0 0 ${w} ${h}`}>{[0,25,50,75,100].map(v=><g key={v}><line x1={p} x2={w-p} y1={y(v)} y2={y(v)} className="grid"/><text x="4" y={y(v)+4}>{v}%</text></g>)}<polyline points={line(1)} className="planned"/><polyline points={line(2)} className="actual"/>{curve.map((d,i)=><circle key={i} cx={x(i)} cy={y(d[2])} r="3.2"/>)}{curve.filter((_,i)=>i%2===0).map((d,i)=><text key={d[0]} x={x(i*2)} y={h-7} className="xaxis">{d[0]}</text>)}</svg></div>}
